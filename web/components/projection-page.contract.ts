import type { ComponentProps } from "react";
import { ProjectionPage } from "./projection-page";

const props: ComponentProps<typeof ProjectionPage> = {
  jobId: "job-1",
  ticker: "THYAO.IS",
};

void props;
