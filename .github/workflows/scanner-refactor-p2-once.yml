name: IZFIN Scanner Refactor P2 Once

on:
  push:
    branches:
      - develop
    paths:
      - .github/workflows/scanner-refactor-p2-once.yml

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout develop
        uses: actions/checkout@v6
        with:
          ref: develop

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Refactor scanner pipeline safely
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          root = Path('.')
          scanner = root / 'izfin_core' / 'scanner_engine.py'
          app = root / 'app2.py'
          tests = root / 'tests' / 'test_scanner_engine.py'
          arch = root / 'tests' / 'test_core_architecture.py'

          scanner_text = scanner.read_text(encoding='utf-8')
          if 'from izfin_core.decision_engine import sinyal_yonu_belirle, volatilite_rejimi' not in scanner_text:
              scanner_text = scanner_text.replace(
                  'import pandas as pd\n',
                  'import pandas as pd\n\nfrom izfin_core.decision_engine import sinyal_yonu_belirle, volatilite_rejimi\nfrom izfin_core.risk_engine import teknik_seviyeler_hesapla\n',
                  1,
              )

          if 'def temel_teknik_gostergeleri_hesapla(' not in scanner_text:
              scanner_text += r'''


def temel_teknik_gostergeleri_hesapla(df_long: pd.DataFrame) -> dict[str, Any]:
    """Tarama döngüsündeki temel teknik serileri tek, saf hesaplama paketinde üretir."""
    close = df_long["Close"]
    high = df_long["High"]
    low = df_long["Low"]
    volume = df_long["Volume"]
    fiyat = float(close.iloc[-1])

    delta = close.diff()
    rs = delta.where(delta > 0, 0.0).ewm(alpha=1 / 14, adjust=False).mean() / (
        -delta.where(delta < 0, 0.0).ewm(alpha=1 / 14, adjust=False).mean() + 1e-5
    )
    rsi = 100 - (100 / (1 + rs)).iloc[-1]

    macd_serisi = close.ewm(span=12, adjust=False).mean() - close.ewm(span=26, adjust=False).mean()
    macd_sinyal = macd_serisi.ewm(span=9, adjust=False).mean()

    sma_200 = close.rolling(200).mean().iloc[-1] if len(df_long) >= 200 else close.mean()
    uzun_vade_trend = fiyat > sma_200

    bb_mid_serisi = close.rolling(20).mean()
    bb_std_serisi = close.rolling(20).std()
    bb_mid = bb_mid_serisi.iloc[-1]
    bb_ust_serisi = bb_mid_serisi + (bb_std_serisi * 2)
    bb_alt_serisi = bb_mid_serisi - (bb_std_serisi * 2)
    bb_ust = bb_ust_serisi.iloc[-1]
    bb_alt = bb_alt_serisi.iloc[-1]
    onceki_bb_ust = bb_ust_serisi.shift(1).iloc[-1]

    typical_price = (high + low + close) / 3
    raw_money_flow = typical_price * volume
    pos_flow = pd.Series(
        np.where(typical_price > typical_price.shift(1), raw_money_flow, 0),
        index=df_long.index,
    )
    neg_flow = pd.Series(
        np.where(typical_price < typical_price.shift(1), raw_money_flow, 0),
        index=df_long.index,
    )
    mfi = 100 - (100 / (1 + (pos_flow.rolling(14).sum() / (neg_flow.rolling(14).sum() + 1e-5))))
    mfi_val = mfi.iloc[-1] if not pd.isna(mfi.iloc[-1]) else 50

    obv = np.where(
        close > close.shift(1),
        volume,
        np.where(close < close.shift(1), -volume, 0),
    ).cumsum()
    obv_ema = pd.Series(obv, index=df_long.index).ewm(span=20, adjust=False).mean()

    ema_9_val = close.ewm(span=9, adjust=False).mean().iloc[-1]
    ema_21_val = close.ewm(span=21, adjust=False).mean().iloc[-1]
    ema_50_val = close.ewm(span=50, adjust=False).mean().iloc[-1]

    return {
        "rsi": float(rsi),
        "macd_serisi": macd_serisi,
        "macd_sinyal": macd_sinyal,
        "macd": float(macd_serisi.iloc[-1]),
        "macd_signal": float(macd_sinyal.iloc[-1]),
        "sma200": float(sma_200),
        "uzun_vade_trend": bool(uzun_vade_trend),
        "bb_mid": float(bb_mid),
        "bb_ust": float(bb_ust),
        "bb_alt": float(bb_alt),
        "onceki_bb_ust": float(onceki_bb_ust),
        "mfi": float(mfi_val),
        "obv": obv,
        "obv_ema": obv_ema,
        "obv_son": float(obv[-1]),
        "obv_ema_son": float(obv_ema.iloc[-1]),
        "ema9": float(ema_9_val),
        "ema21": float(ema_21_val),
        "ema50": float(ema_50_val),
    }


def risk_volatilite_hazirla(
    df_long: pd.DataFrame,
    *,
    fiyat: float,
    ema50: float,
    bb_alt: float,
    bb_mid: float,
    bb_ust: float,
    adx: float,
) -> dict[str, Any]:
    """ATR, tarihsel volatilite, destek/direnç, stop ve teknik hedefleri tek pakette üretir."""
    gecmis_df = df_long.iloc[:-1] if len(df_long) > 1 else df_long
    swing_high = gecmis_df["High"].tail(50).max()
    swing_low = gecmis_df["Low"].tail(50).min()

    tr = pd.concat(
        [
            df_long["High"] - df_long["Low"],
            (df_long["High"] - df_long["Close"].shift()).abs(),
            (df_long["Low"] - df_long["Close"].shift()).abs(),
        ],
        axis=1,
    ).max(axis=1)
    atr = tr[-14:].mean() if len(tr) >= 14 else fiyat * 0.02

    log_getiriler = np.log(df_long["Close"] / df_long["Close"].shift(1)).replace(
        [np.inf, -np.inf], np.nan
    ).dropna()
    hv20 = float(log_getiriler.tail(20).std(ddof=1) * np.sqrt(252)) if len(log_getiriler) >= 20 else 0.0
    hv60 = float(log_getiriler.tail(60).std(ddof=1) * np.sqrt(252)) if len(log_getiriler) >= 30 else hv20
    if not np.isfinite(hv20) or hv20 <= 0:
        hv20 = float((atr / fiyat) * np.sqrt(252)) if fiyat > 0 else 0.20
    if not np.isfinite(hv60) or hv60 <= 0:
        hv60 = hv20

    karma_destek = max(
        [d for d in [swing_low, ema50, fiyat - (atr * 2)] if pd.notna(d) and d < fiyat],
        default=fiyat - (atr * 1.5),
    )
    karma_direnc = min(
        [d for d in [swing_high, bb_ust] if pd.notna(d) and d > fiyat],
        default=fiyat + (atr * 2.5),
    )

    chandelier_stop = gecmis_df["High"].tail(22).max() - (atr * 3)
    stop_adaylari = [
        x
        for x in [chandelier_stop, fiyat - (atr * 1.5), karma_destek - (atr * 0.25)]
        if pd.notna(x) and x < fiyat
    ]
    trailing_stop = max(stop_adaylari, default=fiyat - (atr * 1.5))
    risk_yuzde = (fiyat - trailing_stop) / max(fiyat, 1e-9) * 100
    risk_seviyesi = "YÜKSEK" if risk_yuzde > 7 or adx < 18 else (
        "DÜŞÜK" if risk_yuzde < 3.5 and adx >= 25 else "ORTA"
    )
    vol_rejimi = volatilite_rejimi(fiyat, atr, hv20)

    seviyeler = teknik_seviyeler_hesapla(
        df_long, fiyat, atr, ema50, bb_alt, bb_mid, bb_ust, hv20
    )
    tp1, tp2, tp3 = seviyeler["tp1"], seviyeler["tp2"], seviyeler["tp3"]
    karma_destek, karma_direnc = seviyeler["s1"], seviyeler["r1"]
    risk_odul = (tp2 - fiyat) / max(fiyat - trailing_stop, 1e-9)

    return {
        "swing_high": float(swing_high),
        "swing_low": float(swing_low),
        "atr": float(atr),
        "hv20": float(hv20),
        "hv60": float(hv60),
        "destek": float(karma_destek),
        "direnc": float(karma_direnc),
        "stop": float(trailing_stop),
        "risk_yuzde": float(risk_yuzde),
        "risk_seviyesi": risk_seviyesi,
        "volatilite_rejimi": vol_rejimi,
        "seviyeler": seviyeler,
        "tp1": float(tp1),
        "tp2": float(tp2),
        "tp3": float(tp3),
        "risk_odul": float(risk_odul),
        "hibrit_tp": f"TP1: {tp1:.2f} | TP2: {tp2:.2f} | TP3: {tp3:.2f}",
    }


def teknik_panel_paketi_olustur(
    *,
    ticker: str,
    fiyat: float,
    gunluk_degisim: float,
    temel: dict[str, Any],
    risk: dict[str, Any],
    gelismis: dict[str, Any],
    tetik: dict[str, Any],
    karar: dict[str, Any],
    piyasa: dict[str, Any],
    skor_aciklama: dict[str, Any],
) -> dict[str, Any]:
    """Bir ticker'ın ham analiz bağlamını UI/Firestore tarafından tüketilen tek panel sözlüğüne dönüştürür."""
    seviyeler = risk["seviyeler"]
    sinyal = karar["sinyal"]
    vwap = gelismis.get("vwap", np.nan)

    return {
        "ticker": ticker,
        "fiyat": float(fiyat),
        "gunluk_degisim": float(gunluk_degisim),
        "ema9": float(temel["ema9"]),
        "ema21": float(temel["ema21"]),
        "ema50": float(temel["ema50"]),
        "sma200": float(temel["sma200"]),
        "rsi": float(temel["rsi"]),
        "mfi": float(temel["mfi"]),
        "macd": float(temel["macd"]),
        "macd_signal": float(temel["macd_signal"]),
        "atr": float(risk["atr"]),
        "hv20": float(risk["hv20"]),
        "hv60": float(risk["hv60"]),
        "obv": float(temel["obv_son"]),
        "obv_ema": float(temel["obv_ema_son"]),
        "bb_alt": float(temel["bb_alt"]),
        "bb_mid": float(temel["bb_mid"]),
        "bb_ust": float(temel["bb_ust"]),
        "destek": float(risk["destek"]),
        "direnc": float(risk["direnc"]),
        "stop": float(risk["stop"]),
        "tp1": float(risk["tp1"]),
        "tp2": float(risk["tp2"]),
        "tp3": float(risk["tp3"]),
        "swing_low": float(risk["swing_low"]),
        "swing_high": float(risk["swing_high"]),
        "s1": float(seviyeler["s1"]),
        "s2": float(seviyeler["s2"]),
        "s3": float(seviyeler["s3"]),
        "r1": float(seviyeler["r1"]),
        "r2": float(seviyeler["r2"]),
        "r3": float(seviyeler["r3"]),
        "tp1_yildiz": int(seviyeler["tp1_yildiz"]),
        "tp2_yildiz": int(seviyeler["tp2_yildiz"]),
        "tp3_yildiz": int(seviyeler["tp3_yildiz"]),
        "hacim": float(piyasa["hacim"]),
        "hacim_ort": float(piyasa["hacim_ort"]),
        "hacim_oran": float(piyasa["hacim_oran"]),
        "sektorel_fark": float(piyasa["sektorel_fark"]),
        "sinyal": sinyal,
        "profil": karar["profil"],
        "on_sinyal": karar["on_sinyal"],
        "merkezi_karar": karar["merkezi_karar"],
        "veri_kaynagi": piyasa["veri_kaynagi"],
        "teyit": piyasa["teyit"],
        "tetik_puani": int(tetik.get("puan", 0)),
        "tetik_seviyesi": tetik.get("seviye", "⏳ TETİK YOK"),
        "tetik_detay": tetik.get("detay", []),
        "tetik_direnc": tetik.get("direnc"),
        "tetik_hacim_orani": float(tetik.get("hacim_orani", 0.0)),
        "tetik_rsi": tetik.get("rsi"),
        "tetik_mum_kalitesi": float(tetik.get("mum_kalitesi", 0.0)),
        "tetik_sahte_kirilim": bool(tetik.get("sahte_kirilim", False)),
        "giris_puani": int(tetik.get("puan", 0)),
        "giris_seviyesi": tetik.get("seviye", "⏳ GİRİŞ UYGUN DEĞİL"),
        "giris_asamasi": tetik.get("asama", "YOK"),
        "giris_zaman_dilimleri": tetik.get("zaman_dilimleri", {}),
        "giris_detay": tetik.get("detay", []),
        "adx": float(gelismis["adx"]),
        "plus_di": float(gelismis["plus_di"]),
        "minus_di": float(gelismis["minus_di"]),
        "cmf": float(gelismis["cmf"]),
        "ad_line": float(gelismis["ad_line"]),
        "supertrend": int(gelismis["supertrend"]),
        "supertrend_line": float(gelismis["supertrend_line"]),
        "vwap": float(vwap) if np.isfinite(vwap) else np.nan,
        "mtf_detay": gelismis["mtf_detay"],
        "mtf_uyum": int(gelismis["mtf_uyum"]),
        "guven_skoru": int(gelismis["guven_skoru"]),
        "risk_odul": float(risk["risk_odul"]),
        "risk_yuzde": float(risk["risk_yuzde"]),
        "risk_seviyesi": risk["risk_seviyesi"],
        "volatilite_rejimi": risk["volatilite_rejimi"],
        "sinyal_yonu": sinyal_yonu_belirle(sinyal),
        "cezali_skor": int(skor_aciklama["nihai_skor"]),
        "nihai_skor": int(skor_aciklama["nihai_skor"]),
        "eski_cezali_skor": int(skor_aciklama["eski_skor"]),
        "skor_bonus": int(skor_aciklama["bonus"]),
        "skor_ceza": int(skor_aciklama["ceza"]),
        "skor_aciklama": skor_aciklama,
        "seans_disi": piyasa["seans_disi"],
        "seans_disi_fiyat": piyasa["seans_disi_fiyat"],
    }
'''
          scanner.write_text(scanner_text, encoding='utf-8')

          app_text = app.read_text(encoding='utf-8')

          old_import = '''from izfin_core.scanner_engine import (
    goreceli_guc_ve_hacim_hesapla,
    hibrit_skor_hesapla,
    on_sinyal_belirle,
)'''
          new_import = '''from izfin_core.scanner_engine import (
    goreceli_guc_ve_hacim_hesapla,
    hibrit_skor_hesapla,
    on_sinyal_belirle,
    risk_volatilite_hazirla,
    teknik_panel_paketi_olustur,
    temel_teknik_gostergeleri_hesapla,
)'''
          assert app_text.count(old_import) == 1, 'scanner import block not found exactly once'
          app_text = app_text.replace(old_import, new_import, 1)

          old_technical = '''                        delta = df_long['Close'].diff()
                        rs = delta.where(delta>0, 0.0).ewm(alpha=1/14, adjust=False).mean() / (-delta.where(delta<0, 0.0).ewm(alpha=1/14, adjust=False).mean() + 1e-5)
                        rsi = 100 - (100 / (1 + rs)).iloc[-1]
                        
                        macd_serisi = df_long['Close'].ewm(span=12, adjust=False).mean() - df_long['Close'].ewm(span=26, adjust=False).mean()
                        macd_sinyal = macd_serisi.ewm(span=9, adjust=False).mean()
                        
                        sma_200 = df_long['Close'].rolling(200).mean().iloc[-1] if len(df_long) >= 200 else df_long['Close'].mean()
                        uzun_vade_trend = bugun_kapanis > sma_200

                        bb_mid = df_long['Close'].rolling(20).mean().iloc[-1]
                        bb_ust = (df_long['Close'].rolling(20).mean() + (df_long['Close'].rolling(20).std() * 2)).iloc[-1]
                        bb_alt = (df_long['Close'].rolling(20).mean() - (df_long['Close'].rolling(20).std() * 2)).iloc[-1]

                        typical_price = (df_long['High'] + df_long['Low'] + df_long['Close']) / 3
                        raw_money_flow = typical_price * df_long['Volume']
                        pos_flow = pd.Series(np.where(typical_price > typical_price.shift(1), raw_money_flow, 0), index=df_long.index)
                        neg_flow = pd.Series(np.where(typical_price < typical_price.shift(1), raw_money_flow, 0), index=df_long.index)
                        mfi = 100 - (100 / (1 + (pos_flow.rolling(14).sum() / (neg_flow.rolling(14).sum() + 1e-5))))
                        mfi_val = mfi.iloc[-1] if not pd.isna(mfi.iloc[-1]) else 50
                        
                        obv = np.where(df_long['Close'] > df_long['Close'].shift(1), df_long['Volume'], np.where(df_long['Close'] < df_long['Close'].shift(1), -df_long['Volume'], 0)).cumsum()
                        obv_ema = pd.Series(obv, index=df_long.index).ewm(span=20, adjust=False).mean()
'''
          new_technical = '''                        # Temel teknik seri hazırlığı saf scanner motoruna taşındı.
                        temel_teknik = temel_teknik_gostergeleri_hesapla(df_long)
                        rsi = temel_teknik["rsi"]
                        macd_serisi = temel_teknik["macd_serisi"]
                        macd_sinyal = temel_teknik["macd_sinyal"]
                        sma_200 = temel_teknik["sma200"]
                        uzun_vade_trend = temel_teknik["uzun_vade_trend"]
                        bb_mid = temel_teknik["bb_mid"]
                        bb_ust = temel_teknik["bb_ust"]
                        bb_alt = temel_teknik["bb_alt"]
                        mfi_val = temel_teknik["mfi"]
                        obv = temel_teknik["obv"]
                        obv_ema = temel_teknik["obv_ema"]
                        ema_9_val = temel_teknik["ema9"]
                        ema_21_val = temel_teknik["ema21"]
                        ema_50_val = temel_teknik["ema50"]
                        onceki_bb_ust = temel_teknik["onceki_bb_ust"]
'''
          assert app_text.count(old_technical) == 1, 'technical preparation block not found exactly once'
          app_text = app_text.replace(old_technical, new_technical, 1)

          old_ema50 = '''                        # --- HİBRİT SKOR: SAF SCANNER MOTORU ---
                        ema_50_val = df_long['Close'].ewm(span=50, adjust=False).mean().iloc[-1]
                        skor_aciklama = hibrit_skor_hesapla('''
          new_ema50 = '''                        # --- HİBRİT SKOR: SAF SCANNER MOTORU ---
                        skor_aciklama = hibrit_skor_hesapla('''
          assert app_text.count(old_ema50) == 1, 'duplicate ema50 block not found exactly once'
          app_text = app_text.replace(old_ema50, new_ema50, 1)

          risk_pattern = re.compile(
              r'''                        # Destek/direnç referanslarında mevcut mumu hariç tutmak,\n.*?                        hibrit_tp = f"TP1: \{tp1:\.2f\} \| TP2: \{tp2:\.2f\} \| TP3: \{tp3:\.2f\}"\n''',
              re.S,
          )
          new_risk = '''                        # Risk, volatilite, destek/direnç ve hedef hazırlığı saf scanner motorunda.
                        risk_paketi = risk_volatilite_hazirla(
                            df_long,
                            fiyat=bugun_kapanis,
                            ema50=ema_50_val,
                            bb_alt=bb_alt,
                            bb_mid=bb_mid,
                            bb_ust=bb_ust,
                            adx=adx,
                        )
                        swing_high = risk_paketi["swing_high"]
                        swing_low = risk_paketi["swing_low"]
                        atr = risk_paketi["atr"]
                        hv20 = risk_paketi["hv20"]
                        hv60 = risk_paketi["hv60"]
                        karma_destek = risk_paketi["destek"]
                        karma_direnc = risk_paketi["direnc"]
                        trailing_stop = risk_paketi["stop"]
                        risk_yuzde = risk_paketi["risk_yuzde"]
                        risk_seviyesi = risk_paketi["risk_seviyesi"]
                        vol_rejimi = risk_paketi["volatilite_rejimi"]
                        seviyeler = risk_paketi["seviyeler"]
                        tp1, tp2, tp3 = risk_paketi["tp1"], risk_paketi["tp2"], risk_paketi["tp3"]
                        risk_odul = risk_paketi["risk_odul"]
                        hibrit_tp = risk_paketi["hibrit_tp"]
'''
          app_text, n = risk_pattern.subn(new_risk, app_text, count=1)
          assert n == 1, f'risk block replacement count={n}'

          old_breakout_prep = '''                        ema_9_val = df_long['Close'].ewm(span=9, adjust=False).mean().iloc[-1]
                        ema_21_val = df_long['Close'].ewm(span=21, adjust=False).mean().iloc[-1]
                        bb_ust_serisi = df_long['Close'].rolling(20).mean() + (df_long['Close'].rolling(20).std() * 2)
                        onceki_bb_ust = bb_ust_serisi.shift(1).iloc[-1]
'''
          assert app_text.count(old_breakout_prep) == 1, 'breakout preparation block not found exactly once'
          app_text = app_text.replace(old_breakout_prep, '', 1)

          panel_pattern = re.compile(
              r'''                        gecici_teknik_paneller\[ticker\] = \{\n.*?\n                        \}\n\n                        gecici_sozlu_analizler''',
              re.S,
          )
          new_panel = '''                        # Teknik panel kaydı tek bir saf paketleyicide standardize edilir.
                        gecici_teknik_paneller[ticker] = teknik_panel_paketi_olustur(
                            ticker=ticker,
                            fiyat=bugun_kapanis,
                            gunluk_degisim=gunluk_degisim,
                            temel=temel_teknik,
                            risk=risk_paketi,
                            gelismis={
                                "adx": adx, "plus_di": plus_di, "minus_di": minus_di,
                                "cmf": cmf, "ad_line": ad_line,
                                "supertrend": supertrend, "supertrend_line": supertrend_line,
                                "vwap": vwap, "mtf_detay": mtf_detay, "mtf_uyum": mtf_uyum,
                                "guven_skoru": guven_skoru,
                            },
                            tetik=tetik_sonucu,
                            karar={
                                "sinyal": sinyal, "profil": profil_sinyali,
                                "on_sinyal": on_sinyal, "merkezi_karar": merkezi_karar,
                            },
                            piyasa={
                                "hacim": bugun_hacim, "hacim_ort": hacim_sma20,
                                "hacim_oran": hacim_oran, "sektorel_fark": sektorel_fark,
                                "veri_kaynagi": veri_kaynagi, "teyit": mikro_teyit,
                                "seans_disi": seans_disi_metin, "seans_disi_fiyat": seans_disi_fiyat,
                            },
                            skor_aciklama=skor_aciklama,
                        )

                        gecici_sozlu_analizler'''
          app_text, n = panel_pattern.subn(new_panel, app_text, count=1)
          assert n == 1, f'panel package replacement count={n}'

          app.write_text(app_text, encoding='utf-8')

          tests_text = tests.read_text(encoding='utf-8')
          old_test_import = '''from izfin_core.scanner_engine import (
    goreceli_guc_ve_hacim_hesapla,
    hibrit_skor_hesapla,
    on_sinyal_belirle,
)'''
          new_test_import = '''from izfin_core.scanner_engine import (
    goreceli_guc_ve_hacim_hesapla,
    hibrit_skor_hesapla,
    on_sinyal_belirle,
    risk_volatilite_hazirla,
    teknik_panel_paketi_olustur,
    temel_teknik_gostergeleri_hesapla,
)'''
          assert tests_text.count(old_test_import) == 1, 'scanner test import block not found'
          tests_text = tests_text.replace(old_test_import, new_test_import, 1)

          if 'def _ohlcv_ornek()' not in tests_text:
              tests_text += r'''


def _ohlcv_ornek(periods=240):
    idx = pd.date_range("2025-01-01", periods=periods, freq="D")
    close = np.linspace(80.0, 120.0, periods) + np.sin(np.arange(periods) / 5.0)
    return pd.DataFrame(
        {
            "Open": close - 0.4,
            "High": close + 1.2,
            "Low": close - 1.1,
            "Close": close,
            "Volume": np.linspace(1_000_000, 1_800_000, periods),
        },
        index=idx,
    )


def test_temel_teknik_gostergeleri_are_finite_and_structured():
    sonuc = temel_teknik_gostergeleri_hesapla(_ohlcv_ornek())
    for key in ("rsi", "macd", "macd_signal", "sma200", "bb_mid", "bb_ust", "bb_alt", "mfi", "ema9", "ema21", "ema50"):
        assert np.isfinite(sonuc[key])
    assert len(sonuc["obv"]) == 240
    assert len(sonuc["obv_ema"]) == 240
    assert isinstance(sonuc["uzun_vade_trend"], bool)


def test_risk_volatilite_package_contains_levels_and_bounded_risk_metadata():
    df = _ohlcv_ornek()
    temel = temel_teknik_gostergeleri_hesapla(df)
    risk = risk_volatilite_hazirla(
        df,
        fiyat=float(df["Close"].iloc[-1]),
        ema50=temel["ema50"],
        bb_alt=temel["bb_alt"],
        bb_mid=temel["bb_mid"],
        bb_ust=temel["bb_ust"],
        adx=28.0,
    )
    assert risk["atr"] > 0
    assert risk["stop"] < float(df["Close"].iloc[-1])
    assert risk["risk_seviyesi"] in {"DÜŞÜK", "ORTA", "YÜKSEK"}
    assert risk["volatilite_rejimi"]
    assert {"s1", "s2", "s3", "r1", "r2", "r3"}.issubset(risk["seviyeler"])


def test_teknik_panel_paketi_preserves_core_contract():
    df = _ohlcv_ornek()
    temel = temel_teknik_gostergeleri_hesapla(df)
    risk = risk_volatilite_hazirla(
        df,
        fiyat=float(df["Close"].iloc[-1]),
        ema50=temel["ema50"],
        bb_alt=temel["bb_alt"],
        bb_mid=temel["bb_mid"],
        bb_ust=temel["bb_ust"],
        adx=28.0,
    )
    skor = _skor()
    panel = teknik_panel_paketi_olustur(
        ticker="TEST",
        fiyat=float(df["Close"].iloc[-1]),
        gunluk_degisim=1.25,
        temel=temel,
        risk=risk,
        gelismis={
            "adx": 28.0, "plus_di": 24.0, "minus_di": 14.0,
            "cmf": 0.1, "ad_line": 100.0, "supertrend": 1,
            "supertrend_line": 110.0, "vwap": 118.0,
            "mtf_detay": {}, "mtf_uyum": 75, "guven_skoru": 82,
        },
        tetik={"puan": 70, "seviye": "GÜÇLÜ", "detay": [], "asama": "TEYİTLİ"},
        karar={
            "sinyal": "AL 🟢", "profil": "Trend", "on_sinyal": "KADEMELİ ALIM 🔵",
            "merkezi_karar": {"aksiyon": "AL"},
        },
        piyasa={
            "hacim": 1_800_000, "hacim_ort": 1_700_000, "hacim_oran": 105.0,
            "sektorel_fark": 2.5, "veri_kaynagi": "test", "teyit": "ok",
            "seans_disi": "—", "seans_disi_fiyat": None,
        },
        skor_aciklama=skor,
    )
    assert panel["ticker"] == "TEST"
    assert panel["nihai_skor"] == skor["nihai_skor"]
    assert panel["guven_skoru"] == 82
    assert panel["risk_seviyesi"] == risk["risk_seviyesi"]
    assert panel["giris_puani"] == 70
'''
          tests.write_text(tests_text, encoding='utf-8')

          arch_text = arch.read_text(encoding='utf-8')
          marker = '    "on_sinyal_belirle",\n'
          if '    "temel_teknik_gostergeleri_hesapla",\n' not in arch_text:
              assert marker in arch_text, 'architecture marker not found'
              arch_text = arch_text.replace(
                  marker,
                  marker
                  + '    "temel_teknik_gostergeleri_hesapla",\n'
                  + '    "risk_volatilite_hazirla",\n'
                  + '    "teknik_panel_paketi_olustur",\n',
                  1,
              )
          arch.write_text(arch_text, encoding='utf-8')
          PY

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-test.txt

      - name: Create CI-only Streamlit secrets
        run: |
          mkdir -p "${HOME}/.streamlit"
          cat > "${HOME}/.streamlit/secrets.toml" <<'EOF'
          FINNHUB_API_KEY = ""
          GOOGLE_OAUTH_CLIENT_ID = ""
          GOOGLE_OAUTH_CLIENT_SECRET = ""
          GOOGLE_OAUTH_REDIRECT_URI = ""
          ADMIN_EMAILS = ""
          SENTRY_DSN = ""
          IZFIN_ENVIRONMENT = "ci"
          IZFIN_DATA_CONTROLLER_NAME = ""
          IZFIN_CONTACT_EMAIL = ""
          IZFIN_DATA_CONTROLLER_ADDRESS = ""
          EOF

      - name: Run full test suite before commit
        env:
          FIREBASE_WEB_API_KEY: ""
          FIREBASE_PROJECT_ID: ""
          FINNHUB_API_KEY: ""
          GOOGLE_OAUTH_CLIENT_ID: ""
          GOOGLE_OAUTH_CLIENT_SECRET: ""
          GOOGLE_OAUTH_REDIRECT_URI: ""
          SENTRY_DSN: ""
          IZFIN_ENVIRONMENT: "ci"
        run: python -m pytest -q

      - name: Commit refactor only after tests pass
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app2.py izfin_core/scanner_engine.py tests/test_scanner_engine.py tests/test_core_architecture.py
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No refactor changes detected."
            exit 0
          fi
          git commit -m "Extract scanner technical and risk pipeline"
          git push origin develop
